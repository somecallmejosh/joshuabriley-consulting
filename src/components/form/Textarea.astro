---
import { cx } from '~/lib/cx';

interface Props {
  name: string;
  id?: string;
  placeholder?: string;
  value?: string;
  rows?: number;
  required?: boolean;
  invalid?: boolean;
  describedBy?: string;
  class?: string;
}

const {
  name,
  id,
  placeholder,
  value,
  rows = 5,
  required = false,
  invalid = false,
  describedBy,
  class: className,
} = Astro.props;

const fieldId = id ?? `field-${name}`;
---

<textarea
  id={fieldId}
  name={name}
  rows={rows}
  placeholder={placeholder}
  required={required}
  aria-required={required ? 'true' : undefined}
  aria-invalid={invalid ? 'true' : undefined}
  aria-describedby={describedBy}
  class={cx(
    'w-full bg-raised border border-line rounded-md px-4 py-3 font-body text-sm text-ink placeholder:text-ink-muted resize-none transition-colors focus:border-line-strong aria-[invalid=true]:border-accent',
    className,
  )}
>{value}</textarea>
