---
import { cx } from '~/lib/cx';

interface Props {
  size?: 'xs' | 'sm' | 'base' | 'lg' | 'xl';
  tone?: 'navy' | 'muted' | 'white' | 'whiteMuted' | 'coral' | 'ink' | 'paper' | 'faint' | 'accent';
  weight?: 'normal' | 'medium' | 'semibold';
  align?: 'left' | 'center';
  as?: 'p' | 'span' | 'div';
  leading?: 'tight' | 'snug' | 'normal' | 'relaxed';
  class?: string;
}

const {
  size = 'base',
  tone = 'navy',
  weight = 'normal',
  align = 'left',
  leading = 'relaxed',
  as: Tag = 'p',
  class: className,
} = Astro.props;

const sizes: Record<NonNullable<Props['size']>, string> = {
  xs: 'text-xs',
  sm: 'text-sm',
  base: 'text-base',
  lg: 'text-lg',
  xl: 'text-lg md:text-xl',
};

const tones: Record<NonNullable<Props['tone']>, string> = {
  navy: 'text-ink',
  muted: 'text-ink-muted',
  white: 'text-paper',
  whiteMuted: 'text-paper/70',
  coral: 'text-accent',
  ink: 'text-ink',
  paper: 'text-paper',
  faint: 'text-ink-faint',
  accent: 'text-accent',
};

const weights: Record<NonNullable<Props['weight']>, string> = {
  normal: 'font-normal',
  medium: 'font-medium',
  semibold: 'font-semibold',
};

const leadings: Record<NonNullable<Props['leading']>, string> = {
  tight: 'leading-tight',
  snug: 'leading-snug',
  normal: 'leading-normal',
  relaxed: 'leading-relaxed',
};
---

<Tag
  class={cx(
    'font-body text-pretty',
    sizes[size],
    tones[tone],
    weights[weight],
    leadings[leading],
    align === 'center' && 'text-center',
    className,
  )}
>
  <slot />
</Tag>
