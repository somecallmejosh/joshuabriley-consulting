---
import Container from '~/components/layout/Container.astro';
import Logo from '~/components/primitives/Logo.astro';
import Link from '~/components/primitives/Link.astro';
import MobileNav from '~/components/chrome/MobileNav.astro';
import { PRIMARY_NAV } from '~/lib/nav';
import { cx } from '~/lib/cx';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;

// Hide the header Résumé link when already on the résumé page.
const isResumePage = Astro.url.pathname.replace(/\/$/, '') === '/resume';
---

<header
  class={cx(
    'sticky top-0 z-40 bg-paper/80 backdrop-blur-md border-b border-line',
    className,
  )}
>
  <Container as="div" width="wide" class="flex items-center justify-between gap-6 py-3.5 relative">
    <Logo href="/" variant="lockup" size="md" />

    <nav class="hidden md:flex items-center gap-7" aria-label="Primary">
      {PRIMARY_NAV.filter((l) => l.href !== '/contact/').map((link) => (
        <Link href={link.href} variant="nav">{link.label}</Link>
      ))}
    </nav>

    <div class="hidden md:flex items-center gap-5">
      <span class="mono-label inline-flex items-center gap-1.5">
        <span class="relative flex h-1.5 w-1.5" aria-hidden="true">
          <span class="absolute inline-flex h-full w-full rounded-full bg-accent/60 motion-safe:animate-ping"></span>
          <span class="relative inline-flex h-1.5 w-1.5 rounded-full bg-accent"></span>
        </span>
        Open to product engineering roles
      </span>
      {!isResumePage && (
        <a
          href="/resume/"
          class="mono-label transition-colors hover:text-ink hover:underline underline-offset-4"
        >Résumé</a>
      )}
      <a
        href="/contact/"
        class="inline-flex items-center gap-2 rounded-[5px] bg-ink px-4 py-2 font-sans text-[0.85rem] font-medium tracking-tight text-paper transition-[background-color,transform] duration-300 ease-[cubic-bezier(0.32,0.72,0,1)] hover:bg-ink-soft hover:-translate-y-px active:scale-[0.97] active:translate-y-0"
      >Get in touch</a>
    </div>

    <MobileNav links={PRIMARY_NAV} />
  </Container>
</header>
